@model Stardust.Core.Services.ConfigurationReader.EnvironmentConfig

@{
    ViewBag.Title = "EditEnvironment";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>EditEnvironment</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>EnvironmentConfig</legend>

        @Html.HiddenFor(model => model.Id)

        <div class="editor-label">
            @Html.LabelFor(model => model.EnvironmentName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.EnvironmentName)
            @Html.ValidationMessageFor(model => model.EnvironmentName)
        </div>
        <p>Add or update environmental parameter</p>
        <p>Key: <input type="text" id="keyName" name="keyName"/></p>
        <p>Value: <input type="text" id="keyValue" name="keyValue"/></p>
        @foreach (var p in Model.Parameters)
        {
            <p>@p.Name : @p.Value</p>
        }
        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index",new {id=ViewBag.Id})
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
