@model IEnumerable<Stardust.Core.Services.ConfigurationReader.ServiceConfig>

@{
    ViewBag.Title = "View1";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Services</h2>

<p>
    @Html.ActionLink("Create New", "CreateEnvironment",new {id=ViewBag.SetName})
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ServiceName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ServiceName)
        </td>
        <td>
            @Html.ActionLink("Edit", "EditEnvironment", new {id=ViewBag.SetName, name=item.ServiceName}) 
        </td>
    </tr>
}

</table>
