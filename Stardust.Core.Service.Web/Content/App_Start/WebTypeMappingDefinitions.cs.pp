using Stardust.Core.TypeMapping;

namespace $rootnamespace$.App_Start
{
    public class WebTypeMappingDefinitions : IMappingDefinition
    {
        public void Register()
        {
            //Add type mappings here
            //MapFactory.CreateMapRule<SourceType,TargetType>().Add(s=>s.SourceProperty,t=>t.TargetProperty);
        }
    }
}
