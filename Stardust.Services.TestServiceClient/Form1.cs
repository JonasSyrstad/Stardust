using System;
using System.Windows.Forms;
//using Pragma.Services.Test;
using Stardust.Interstellar;
using Stardust.Nucleus;

namespace Pragma.Services.TestServiceClient
{
    public partial class Form1 : Form
    {
        public Form1()
        {
            InitializeComponent();
        }

        private void button1_Click(object sender, EventArgs e)
        {
//            var runtime = RuntimeFactory.CreateRuntime(Scope.PerRequest);
//            using (var service = runtime.CreateServiceProxy<IService1>())
//            {
//                MessageBox.Show(string.Format("Wcf service returned {0}", service.GetClient().GetData(666)));
//            }
        }
    }
}
